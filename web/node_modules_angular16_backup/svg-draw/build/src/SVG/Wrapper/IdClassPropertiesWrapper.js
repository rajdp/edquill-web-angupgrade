export var idClassPropertiesWrapper = function (element) {
    return function (domElement) {
        if (element.domId) {
            domElement.setAttribute('id', "" + element.domId);
        }
        if (element.class) {
            domElement.setAttribute('class', "" + element.class);
        }
        domElement.setAttribute('data-id', "" + element.id);
        return domElement;
    };
};
//# sourceMappingURL=IdClassPropertiesWrapper.js.map