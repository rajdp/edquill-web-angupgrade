export var colorPropertiesWrapper = function (element) {
    return function (domElement) {
        if (element.fill) {
            domElement.setAttribute('fill', "" + element.fill);
        }
        if (element.stroke) {
            domElement.setAttribute('stroke', "" + element.stroke);
        }
        return domElement;
    };
};
//# sourceMappingURL=ColorPropertiesWrapper.js.map