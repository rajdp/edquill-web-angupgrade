import { expect } from 'chai';
import { spy } from 'sinon';
import { idClassPropertiesWrapper } from '../../src/SVG/Wrapper/IdClassPropertiesWrapper';
describe('IdClassPropertiesWrapper', function () {
    var element = {
        id: 0,
        type: 'circle',
        domId: 'testDomId',
        class: 'testDomClass'
    };
    var domElement = {
        id: 'test',
        setAttribute: spy()
    };
    beforeEach(function () {
        domElement.setAttribute.resetHistory();
    });
    it('should return object', function () {
        var result = idClassPropertiesWrapper(element)(domElement);
        expect(result).deep.equal(domElement);
    });
    it('should set id, class and data-id properties', function () {
        idClassPropertiesWrapper(element)(domElement);
        var expectedSetAttributeArgs = [['id', 'testDomId'], ['class', 'testDomClass'], ['data-id', '0']];
        var setAttributeArgs = domElement.setAttribute.getCalls().map(function (call) { return call.args; });
        expect(expectedSetAttributeArgs).deep.equal(setAttributeArgs);
    });
});
//# sourceMappingURL=IdClassPropertiesWrapper.test.js.map