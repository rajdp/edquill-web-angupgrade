import { expect } from 'chai';
import { spy } from 'sinon';
import { colorPropertiesWrapper } from '../../src/SVG/Wrapper/ColorPropertiesWrapper';
describe('ColorPropertiesWrapper', function () {
    var element = {
        id: 0,
        type: 'circle',
        fill: 'white',
        stroke: 'black'
    };
    var domElement = {
        id: 'test',
        setAttribute: spy()
    };
    beforeEach(function () {
        domElement.setAttribute.resetHistory();
    });
    it('should return object', function () {
        var result = colorPropertiesWrapper(element)(domElement);
        expect(result).deep.equal(domElement);
    });
    it('should set fill and stroke properties', function () {
        colorPropertiesWrapper(element)(domElement);
        var expectedSetAttributeArgs = [['fill', 'white'], ['stroke', 'black']];
        var setAttributeArgs = domElement.setAttribute.getCalls().map(function (call) { return call.args; });
        expect(expectedSetAttributeArgs).deep.equal(setAttributeArgs);
    });
});
//# sourceMappingURL=ColorPropertiesWrapper.test.js.map