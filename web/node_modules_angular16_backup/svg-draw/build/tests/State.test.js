import { expect } from 'chai';
import { spy } from 'sinon';
import { initialState } from '../src/State';
describe('InitialState', function () {
    var stateToSvgChanger = spy(function () { });
    var newState = { elements: [{ test: 'test' }] };
    var actionElement = { id: 1 };
    var stateAction = function (state, setState) {
        setState(newState);
        return actionElement;
    };
    beforeEach(function () {
        stateToSvgChanger.resetHistory();
    });
    it('should return element from action', function () {
        var result = initialState(stateToSvgChanger)(stateAction);
        expect(result).deep.equal(actionElement);
    });
    it('should pass setState to action', function () {
        initialState(stateToSvgChanger)(stateAction);
        var setStateArgs = stateToSvgChanger.getCalls().map(function (call) { return call.args; });
        var expectedSetStateArgs = [[newState]];
        expect(setStateArgs).deep.equal(expectedSetStateArgs);
    });
});
//# sourceMappingURL=State.test.js.map