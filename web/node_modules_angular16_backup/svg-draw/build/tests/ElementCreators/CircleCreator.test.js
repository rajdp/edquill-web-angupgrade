import { expect } from 'chai';
import { spy } from 'sinon';
import { circleCreator } from '../../src/SVG/CircleCreator';
describe('CircleCreator', function () {
    var setAttribute = spy();
    var sampleElement = { setAttribute: setAttribute };
    var createElementNS = spy(function () { return sampleElement; });
    var document = { createElementNS: createElementNS };
    var element = {
        id: 0,
        type: 'circle',
        x: 10,
        y: 10,
        width: 10,
        height: 10
    };
    beforeEach(function () {
        setAttribute.resetHistory();
        createElementNS.resetHistory();
    });
    it('should return object', function () {
        var result = circleCreator(document, element);
        expect(result).deep.equal(sampleElement);
    });
    it('should call createElementNS', function () {
        circleCreator(document, element);
        var expectedCreateElementArgs = [['http://www.w3.org/2000/svg', 'circle']];
        var createElementArgs = createElementNS.getCalls().map(function (call) { return call.args; });
        expect(expectedCreateElementArgs).deep.equal(createElementArgs);
    });
    it('should call setAttribute', function () {
        circleCreator(document, element);
        var expectedSetAttributeArgs = [['cx', '15'], ['cy', '15'], ['r', '5']];
        var setAttributeArgs = setAttribute.getCalls().map(function (call) { return call.args; });
        expect(expectedSetAttributeArgs).deep.equal(setAttributeArgs);
    });
    it('should throw coordinates exception', function () {
        var incorrectElement = {};
        expect(function () { return circleCreator(document, incorrectElement); }).throw('Element x or y are not specified');
    });
    it('should throw size exception', function () {
        var incorrectElement = {
            x: 10,
            y: 10
        };
        expect(function () { return circleCreator(document, incorrectElement); }).throw('Element width or height are not specified');
    });
    it('should throw not equal width and height exception', function () {
        var incorrectElement = {
            x: 10,
            y: 10,
            width: 20,
            height: 10
        };
        expect(function () { return circleCreator(document, incorrectElement); }).throw('Circle width and height are not the same');
    });
});
//# sourceMappingURL=CircleCreator.test.js.map